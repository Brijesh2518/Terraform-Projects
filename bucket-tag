# Configure AWS Provider
provider "aws" {
  region = "ap-south-1"
}

# Create S3 Bucket
resource "aws_s3_bucket" "example" {
  bucket = "my-tf-test-bucket-123456"  # must be globally unique

  tags = {
    Name        = "My bucket"
    Environment = "Dev"
  }
}
